---
export interface Project {
  title: string;
  description: string;
  technologies: string[];
  link?: string;
  github?: string;
}

interface Props {
  projects?: Project[];
}

const { projects = [
  {
    title: "Sistema de Gestión Escolar",
    description: "Sistema completo de gestión escolar con frontend y backend. CRUD de estudiantes, cursos e inscripciones. API RESTful con Spring Boot y interfaz moderna con React.",
    technologies: ["Spring Boot", "React", "MySQL", "Vite", "Bootstrap"],
    link: "https://subkhht.github.io/school-management-system/",
    github: "https://github.com/Subkhht/school-management-system"
  },
  {
    title: "Pokédex",
    description: "Una Pokédex funcional con todos los 1025 Pokémon actuales. Búsqueda por nombre, visualización de detalles, tipos con badges coloridos y diseño inspirado en la Pokédex clásica.",
    technologies: ["HTML", "CSS", "JavaScript", "PokéAPI"],
    link: "https://subkhht.github.io/Pokedex_Subkht/",
    github: "https://github.com/Subkhht/Pokedex_Subkht"
  },
  {
    title: "Hablando con Niños",
    description: "Web interactiva para que niños pequeños (1-5 años) desarrollen su lenguaje. 4 idiomas, +60 palabras, modos exploración y juego con síntesis de voz nativa.",
    technologies: ["React", "Vite", "Web Speech API", "CSS"],
    link: "https://subkhht.github.io/hablando-con-ninos/",
    github: "https://github.com/Subkhht/hablando-con-ninos"
  },
  {
    title: "DigiMemo",
    description: "Aplicación web para explorar y visualizar información detallada de Digimon. Interfaz limpia y funcional para fans de la franquicia.",
    technologies: ["HTML", "CSS", "JavaScript"],
    link: "https://subkhht.github.io/DigiMemo/",
    github: "https://github.com/Subkhht/DigiMemo"
  },
  {
    title: "RUN RUN RUN!!!",
    description: "Videojuego Auto Runner desarrollado con Godot donde eres una barra de pan. Incluye múltiples niveles, música y mecánicas de juego divertidas.",
    technologies: ["Godot", "GDScript"],
    link: "https://github.com/Subkhht/run-run-run-/releases",
    github: "https://github.com/Subkhht/run-run-run-"
  }
] } = Astro.props;
---

<section id="projects" class="projects">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">
        <span class="section-icon">◼</span>
        Proyectos Guardados
      </h2>
      <p class="section-subtitle">Como saves en una Memory Card de PS2</p>
    </div>
    
    <div class="projects-grid">
      {projects.map((project, index) => (
        <article class="project-card ps2-card fade-in" style={`animation-delay: ${index * 0.1}s`}>
          <div class="card-header">
            <div class="card-icon">💾</div>
            <div class="card-info">
              <h3 class="project-title">{project.title}</h3>
            </div>
          </div>
          
          <p class="project-description">{project.description}</p>
          
          <div class="project-tech">
            {project.technologies.map(tech => (
              <span class="tech-tag">{tech}</span>
            ))}
          </div>
          
          <div class="project-actions">
            {project.link && (
              <a href={project.link} class="ps2-button" target="_blank" rel="noopener noreferrer">
                Ver Demo
              </a>
            )}
            {project.github && (
              <a href={project.github} class="ps2-button" target="_blank" rel="noopener noreferrer">
                GitHub
              </a>
            )}
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .projects {
    padding: var(--spacing-xl) 0;
    min-height: 100vh;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--spacing-xl);
  }

  .section-title {
    font-size: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
  }

  .section-icon {
    color: var(--ps2-blue-glow);
    font-size: 2rem;
    animation: glow 2s ease-in-out infinite;
  }

  .section-subtitle {
    color: var(--ps2-blue-light);
    font-size: 1.1rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-lg);
  }

  .project-card {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
    min-height: 320px;
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    padding-bottom: var(--spacing-sm);
    border-bottom: 1px solid var(--ps2-card-border);
  }

  .card-icon {
    font-size: 2rem;
    filter: drop-shadow(0 0 8px var(--ps2-blue-light));
  }

  .card-info {
    flex: 1;
  }

  .project-title {
    font-size: 1.3rem;
    margin-bottom: 0.25rem;
    color: var(--ps2-white);
  }

  .project-size {
    font-size: 0.85rem;
    color: var(--ps2-blue-light);
    font-family: monospace;
  }

  .project-description {
    flex: 1;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.6;
  }

  .project-tech {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-xs);
  }

  .tech-tag {
    background: rgba(77, 148, 255, 0.2);
    color: var(--ps2-blue-light);
    padding: 0.35rem 0.75rem;
    border-radius: 4px;
    font-size: 0.85rem;
    border: 1px solid var(--ps2-blue-light);
    font-weight: 500;
  }

  .project-actions {
    display: flex;
    gap: var(--spacing-sm);
    margin-top: auto;
  }

  .project-actions .ps2-button {
    flex: 1;
    text-align: center;
    font-size: 0.85rem;
    padding: var(--spacing-xs) var(--spacing-sm);
  }

  @media (max-width: 768px) {
    .projects-grid {
      grid-template-columns: 1fr;
    }

    .section-title {
      font-size: 2rem;
      flex-direction: column;
    }
  }
</style>
